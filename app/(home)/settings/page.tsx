import ComingSoon from '@/components/ComingSoon';

export const metadata = {
    title: 'Settings',
    description: 'Settings.',
};

export default function SettingsPage() {
    return <ComingSoon title="환경 설정" />;
}